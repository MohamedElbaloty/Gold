PORT=5000
# Railway (one service): MONGODB_URI + JWT_SECRET required in production
MONGODB_URI=
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d
NODE_ENV=development

# CORS: * = allow all; or set your frontend URL if separate. One service = same origin, * is fine
CORS_ORIGIN=*

# Gold/metal prices — optional: set one for live prices; otherwise fallback prices are used
# GOLDAPI_KEY from https://www.goldapi.io — or METALS_API_KEY from https://metals-api.com
GOLDAPI_KEY=
# METALS_API_KEY=
USD_TO_SAR_RATE=3.75

# News feed APIs (optional — without keys, news page uses Kitco RSS only)
# 1) NewsAPI.org — register: https://newsapi.org/register — then paste key below
NEWSAPI_API_KEY=
# 2) Mediastack — register: https://mediastack.com/signup — then paste key below
MEDIASTACK_API_KEY=
